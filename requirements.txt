# requirements.txt — Morrigan v0.1
# Architecture IA Modulaire Post-LLM

# Core ML
torch>=2.0
ncps                    # Neural Circuit Policies (Liquid Neural Networks)
sentence-transformers   # Embeddings locaux
numpy

# Base de donnees & Memoire
supabase               # Client Supabase (pgvector) — backend optionnel

# Moteur symbolique
pyDatalog              # Moteur logique symbolique
networkx               # Graphes de connaissances

# Generation de texte (Phase 1)
jinja2                 # Templates pour la generation de texte

# Verifieurs Morrigan-Code (Phase 2)
sqlparse               # Parser SQL (structure ; cf. SqlVerifier pour limitations)
tinycss2               # Parser CSS3 utilise par CssVerifier

# Generation neuronale Scathach (Phase 3)
# Wheel CPU prebuilt servi par l'index abetlen → pas de build cmake.
# pip/uv prennent le wheel binaire plutot que le sdist PyPI.
--extra-index-url https://abetlen.github.io/llama-cpp-python/whl/cpu
llama-cpp-python       # Backend RWKV via llama.cpp (modele GGUF, CPU)

# Interface
python-telegram-bot    # Interface Telegram

# Configuration & Utilitaires
pyyaml                 # Configuration YAML
python-dotenv          # Chargement du .env (token Telegram, cles…)

# Ingestion a l'echelle (Phase 5)
datasets               # Streaming du dump Wikipedia FR (wikimedia/wikipedia)

# API HTTP (Phase 5)
fastapi                # API REST par-dessus AnDagda (POST /query + SSE)
uvicorn[standard]      # Serveur ASGI (production : uvicorn[standard])
